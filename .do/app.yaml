# DigitalOcean App Platform — static site
# Create app from this repo, or: doctl apps create --spec .do/app.yaml
name: rebekah-cleans-lane-county
static_sites:
  - name: web
    github:
      repo: ndybiehl/rebekah-cleans-lane-county
      branch: main
      deploy_on_push: true
    source_dir: /
    output_dir: /
    index_document: index.html
    error_document: index.html
    routes:
      - path: /
